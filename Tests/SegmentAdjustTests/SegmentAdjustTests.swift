    import XCTest
    @testable import SegmentAdjust

    final class SegmentAdjustTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SegmentAdjust().text, "Hello, World!")
        }
    }
